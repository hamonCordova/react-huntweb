import React from 'react';
import './styles.css';

const Header = (props) => {
    return (
        <header className="Header">
            <h1>Huntweb</h1>
        </header>
    )
}

export default Header;
